# Optional OpenAI/Codex-friendly agent manifest for EVO.
#
# There is no single universal YAML schema across all agent runners.
# Treat this file as a lightweight, human-readable "agent card" that
# points at the canonical EVO instructions in `skills/evo/SKILL.md`.
#
# If your runner expects different keys, rename/transform as needed.

name: evo
display_name: EVO
description: Explicit-assumption, non-redundant coding workflow (EVO).
entrypoint:
  type: skill
  skill_path: skills/evo/SKILL.md
guidance:
  - Make assumptions explicit and validate quickly.
  - Avoid redundant commands and duplicated work.
  - Prefer minimal root-cause fixes, then validate and stop.
  - Ask before destructive or irreversible operations.

